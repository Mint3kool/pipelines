apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  labels:
    resource: pipeline
    tekton.dev/pipeline: tay-pipeline
  generateName: tay-pipeline-
  namespace: tay-test
spec:
  params:
  - name: git-url
    value: git@github.com:Mint3kool/quarkus-demo.git
  - name: git-revision
    value: main
  - name: IMAGE
    value: tay-image
  pipelineRef:
    name: tay-pipeline
  serviceAccountName: pipeline
  timeout: 1h0m0s
  workspaces:
  - name: ssh-creds
    secret:
      secretName: my-ssh-credentials
  - name: shared-workspace
    persistentVolumeClaim:
      claimName: pipeline-pv
